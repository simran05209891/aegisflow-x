name: Auth Service CI

on:
  push:
    paths:
      - "backend/auth-api/**"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Docker image
        run: |
          docker build -t aegisflow-auth backend/auth-api
